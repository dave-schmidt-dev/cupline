# Format: <path> <max-lines> <reason>. The default limit is 500 lines.
# The reason must explain why the file cannot reasonably be split (for example,
# generated code or one cohesive table); "not refactored yet" is not a reason.
