# Format: <path> <reason>. Source and test files target 500 lines; commits warn above 500
# and fail above 800 unless the file is listed here. A reason must say why the file cannot
# reasonably be split; "not refactored yet" is not one. Legacy entries record files already
# over 800 lines when this rule was adopted: split them when next substantially changed,
# then delete the entry. Entries record a reason, never a line cap.
